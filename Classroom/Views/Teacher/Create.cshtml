@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = "_Layout";
}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-7">
            <div class="card shadow-lg border-0 rounded-lg mt-5">
                <div class="card-header"><h3 class="text-center font-weight-light my-4">Create Teacher</h3></div>
                <div class="card-body">
                    @using (Html.BeginForm("Create", "Teacher", FormMethod.Post, new { @class = "form" }))
                    {
                        <form>
                            <div class="row mb-3">
                                <div class="col-md-6">
                                    <div class="form-floating mb-3 mb-md-0">
                                        @Html.TextBox("Firstname", null, new { @class = "form-control", id = "inputFirstname", placeholder = "John" })
                                        <label for="inputFirstname">Firstname</label>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        @Html.TextBox("Lastname", null, new { @class = "form-control", id = "inputLastname", placeholder = "Doe" })
                                        <label for="inputLastname">Lastname</label>
                                    </div>
                                </div>
                            </div>
                            <div class="form-floating mb-3">
                                @Html.TextBox("Email", null, new { @class = "form-control", id = "inputEmail", placeholder = "Email@someone.com" })
                                <label for="inputEmail">Email</label>
                            </div>
                            <div class="mt-4 mb-0">
                                <div class="d-grid">
                                    <button type="submit" class="btn btn-primary">Create Teacher</button>
                                </div>
                            </div>
                        </form>
                        <!-- Form for creating a class -->
                    }

                </div>
                <div class="card-footer text-center py-3">
                    @if (ViewBag.ErrorMessage != null)
                    {
                        <div class="alert alert-danger" role="alert">
                            @ViewBag.ErrorMessage
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>